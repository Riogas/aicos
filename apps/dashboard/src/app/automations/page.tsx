import { AutomationsClient } from "./client";

export const dynamic = "force-dynamic";
export const metadata = { title: "AICOS · Automatizaciones" };

export default function AutomationsPage() {
  return <AutomationsClient />;
}
